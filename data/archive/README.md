# Data Archive

This directory is for local-only large archives and extraction inputs.

Current expected use:

- `preliminary_or_field_geopackage.7z`
  - source archive for `scripts/extract_oregon_data.py`

Small tracked reference assets no longer live under `data/`. They now live in `reference/`.
