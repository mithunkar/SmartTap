# Reference Assets

This directory holds tracked small static assets that the runtime code and handoff docs depend on.

Current contents:

- AgriMet station metadata
- CDL crop code crosswalk
- OpenET variable keyword mappings
- Crop keyword mappings
- Small OpenET reference documents that are suitable for git

Heavy datasets do not belong here. They stay in `data/`.
